'''
In this project, we will be using LLM models locally and having the model installed on our machine.
We will be using Ollama, which is a platform for running LLM models locally. 
We will be using the gpt-4.1-mini model, which is a smaller version of the GPT-4 model.

To install Ollama, you can follow the instructions on their website: https://ollama.com/docs/installation.

WHen the ollama is installed, you can run the following command to pull the gpt-4.1-mini model:
    >> ollama pull gpt-4.1-mini
After pulling the model, you can run the following command to start the Ollama server:
    >> ollama run phi3:mini 



This model is a smaller version of the GPT-4 model, which is designed to be more efficient and 
faster than the full GPT-4 model. It is suitable for tasks that do not require the full capabilities 
of the GPT-4 model, such as simple text generation or basic question answering.


Building API with Ollama and Langchain
To build an API with Ollama and Langchain, we can use the following code:


'''